#!/bin/sh

case "$1" in
  start|stop|status|restart|reload|force-reload)
    sudo /etc/init.d/php<%= @version %>-fpm $1
  ;;

  deploy)
    sudo /etc/init.d/php<%= @version %>-fpm reload
  ;;

  *)
    echo "Usage: $0 {{start|stop|status|restart|deploy|reload|force-reload}}"
    exit 1
  ;;

esac
